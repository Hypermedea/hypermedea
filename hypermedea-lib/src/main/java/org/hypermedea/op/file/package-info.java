/**
 * <p>
 *   File binding, with support for GET, PUT, POST and DELETE (read, write, append and delete).
 * </p>
 * <p>
 *   By default, if a URI is relative (i.e. if it has no scheme), it is interpreted as a relative
 *   path and any operation on that URI will be handled by the file binding.
 * </p>
 */
package org.hypermedea.op.file;