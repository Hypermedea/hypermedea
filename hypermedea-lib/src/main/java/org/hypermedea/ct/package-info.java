/**
 * Representation handler package, with common interface and base implementations.
 * The package's entry point is {@link org.hypermedea.ct.RepresentationHandlers}, a factory
 * class to uniformly create representation handlers. Default handlers are registered
 * for plain text, JSON and RDF (Turtle, N-Triples, JSON-LD, RDF/XML and their quads variant).
 */
package org.hypermedea.ct;